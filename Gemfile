source "https://rubygems.org"

# Use GitHub Pages gem which includes compatible versions of Jekyll and plugins
gem "github-pages", group: :jekyll_plugins

# If you want to test locally with the exact versions GitHub Pages uses
gem "jekyll-remote-theme"
